# Core dependencies (required)
PyPDF2>=3.0.0
reportlab>=4.0.0
openai>=1.0.0
python-dotenv>=1.0.0

# Model training dependencies (required for custom models)
torch>=2.1.0
transformers>=4.35.0
datasets>=2.14.0
sentencepiece>=0.1.99

# Data processing (required for synthetic data generation)
pyarrow>=14.0.0
pandas>=2.0.0
numpy>=1.24.0

# Optimization libraries (highly recommended for best performance)
# These provide 100x+ speedups when available
numba>=0.58.0
xgboost>=2.0.0
optuna>=3.4.0
mlflow>=2.8.0

# GPU acceleration (optional, but provides massive speedups on NVIDIA GPUs)
# Uncomment if you have CUDA-compatible GPU:
# cudf-cu11>=23.10.0; platform_system == "Linux"
# cupy-cuda11x>=12.0.0; platform_system == "Linux"


