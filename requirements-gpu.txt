# GPU-Accelerated dependencies for maximum performance
# Install with: pip install -r requirements.txt -r requirements-gpu.txt

# RAPIDS (GPU-accelerated data processing)
# Provides 104x speedup for I/O, 26x for ML inference
cudf-cu11>=23.10.0; platform_system == "Linux"
cupy-cuda11x>=12.0.0

# Note: Adjust cuda version (cu11, cu12) based on your CUDA installation
# Check with: nvidia-smi
# For CUDA 12.x, use cu12 packages instead of cu11
